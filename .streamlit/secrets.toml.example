# Copy to .streamlit/secrets.toml locally, or paste into Streamlit Community Cloud's
# "Secrets" settings for the deployed app. Optional — the GenAI Copilot works without
# this key using a deterministic local backend.
ANTHROPIC_API_KEY = "sk-ant-..."
